initpop <- function(PopArray){
        
        source('C:/Users/a0091335/Desktop/Work/PhD/Manuscripts/Manuscript-MODE and NSGA/IMODE/HEN retrofitting using IMODE and R/variables_file_ex3.R')
        
        # initialize the population using random number
        # uniform initialization
        
        type <- dVtype
        nVar <- length(VLB)
        
        lb <- VLB
        ub <- VUB
        
        popsize <- length(PopArray$var1)
        
        for (1 in 1:popsize){
                
                for (j in 1:nVar){
                        
                        PopArray$var1
                }
        }
}